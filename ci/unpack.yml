---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: onsdigital/dp-concourse-tools-git

inputs:
  - name: build-bundle
  - name: the-train

outputs:
  - name: build
  - name: target

run:
  path: the-train/ci/scripts/unpack.sh
