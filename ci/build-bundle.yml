---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: onsdigital/dp-concourse-tools-git

inputs:
  - name: artifacts
  - name: the-train

outputs:
  - name: build

run:
  path: the-train/ci/scripts/build-bundle.sh
