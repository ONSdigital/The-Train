---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven
    tag: 3.5.0-jdk-8

inputs:
  - name: the-train

outputs:
  - name: build

run:
  path: the-train/ci/scripts/build.sh
